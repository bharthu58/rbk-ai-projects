Your goal is to ​update them if there are any vulnerable dependencies.

 DO the following:
 
 1. Run `np audit` to find vulnerable installed packages in this project
 2. Run `npm audit fix` to apply updates
 3. Run tests and verify the updates didnt break anything