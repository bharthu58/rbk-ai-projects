**Session preamble — read this first.**

`E:\RBK_AUR_DSKTP_AI\Workspace\aoe-gateway\State.md` is this repo's authoritative handoff file. At session start, read it before anything else. Execute its Next actions in order unless told otherwise.

Before finishing a session: append what changed to the Decisions log, refresh Open items, and rewrite Next actions so the next session can start from "Sync from State.md" alone.

Also read `E:\RBK_AUR_DSKTP_AI\Workspace\AI-Operating-Environment.md` — the global contract. The gateway is the AI Orchestrator: deterministic plumbing, no LLM calls, detection not diagnosis.
